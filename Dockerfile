# Use the official Ubuntu as the base image
FROM ubuntu:latest

# Optionally, set environment variables or run additional commands
# For example, you can install packages or configure your image further
# RUN apt-get update
# RUN apt-get install -y package-name

# (Optional) Set a default command to run when the container starts
# CMD ["command", "arguments"]

